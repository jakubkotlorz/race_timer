# Race Timer

Training project for practicing IPC in C++.

The system consists of two applications:

- `race_timer_server` – collects lap times and maintains ranking
- `race_timer_client` – sends timing events to the server

Goal: simulate real-time race timing similar to motorsport telemetry systems.